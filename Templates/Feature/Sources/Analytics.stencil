import Foundation

//sourcery: AutoMockable
protocol {{ NAME }}AnalyticsProtocol {
    func screenView()
}

class {{ NAME }}Analytics: {{ NAME }}AnalyticsProtocol {
    //private let analytics = FirebaseAnalytics<{{ NAME }}AnalyticsEvents>()

    func screenView() {
        //analytics.pageview(screenClass: {{ NAME }}ViewController.identifier)
        //analytics.log(.view)
    }
}